---
description: Quick metabrain status check — fuel, in-progress tasks, thought stack
disable-model-invocation: true
---

Call `mcp__nexus__nexus_brief` with the current project to get the full state, then summarize in 3 lines:

1. Fuel: session X% | weekly Y%
2. Active: N in-progress, M in backlog
3. Thoughts: N held (or "stack empty")

Be terse — this is a quick glance, not a full briefing.
